@page "/modeles"
@inject NavigationManager NavigationManager

<PageTitle>Véhicule</PageTitle>

<div class="container">
    <div class="row">
        <div class="col">
            <!-- Texte "Véhicules en retard d'entretien" -->
            <div class="mt-3 d-flex justify-content-between align-items-center">
                <h2 class="mb-0">Modèles</h2>
                <button class="btn btn-primary" @onclick="AjouterModele">+</button>

            </div>
            <!-- Tableau -->
            <div class="mt-3">
                <table class="table table-striped table-bordered table-hover">
                    <thead class="thead-dark">
                        <tr>
                            <th>Marque</th>
                            <th>Modèle</th>
                            <th>Fréquence</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@code {
    private void AjouterModele()
    {
        // Ajouter la logique pour ajouter un nouveau véhicule ici
    }
}